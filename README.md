Aqua-Seal is a USSD-first fish traceability and micro-marketplace platform for Lake Victoria fishers, BMUs, fishmongers, and buyers. It requires no blockchain and no smartphone for the core flow.

## Repository structure
```text
aqua-seal/
├── backend/{cmd/api,internal/{api,models,services,repository,database,security,config},migrations,tests}/.gitkeep
├── frontend/{app/{verify,dashboard,bmu,marketplace,login},components,lib,hooks,types,public}/.gitkeep
├── docs/{architecture/diagrams,api,product,development,decisions}/.gitkeep
├── infrastructure/{docker,deployment}/.gitkeep
├── scripts/.gitkeep
└── .github/workflows/.gitkeep
```

Status: Repository structure scaffolded. Architecture, ADRs, and implementation to follow.
